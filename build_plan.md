# ПЛАН РОЗРОБКИ MVP — «Продюсер у кишені» (Юстина)

> Від готового дизайну → до робочого бота на сервері.
> Основа: agent_bot.py (агент-цикл, vision, нотатки, історія вже є).
> Шліфуємо в процесі. Кожна фаза = робочий крок, який можна тестувати.

## ЩО ВЖЕ Є (технічно)
```
✅ agent_bot.py — агент-цикл з tools, vision (фото), нотатки по user_id,
   історія, read_url, дата/час. python-telegram-bot + anthropic SDK.
✅ Сервер: VPS, mybot.service (systemd), автодеплой (git pull щохвилини).
✅ .env: ANTHROPIC_API_KEY, TELEGRAM_BOT_TOKEN.
⚠️ SYSTEM_PROMPT зараз generic ("бізнес-асистент") — треба замінити.
⚠️ Історія в пам'яті (втрачається при рестарті) — треба на диск.
⚠️ Модель у коді "opus-4-7"/"sonnet-4-6" — звірити актуальний id, взяти
   сильну (якість контенту критична).
```

## ФАЗА 0 — ЖИВА ЮСТИНА (найменший робочий бот)
```
• Взяти agent_bot.py за головний файл (bot.py) — консолідувати.
• SYSTEM_PROMPT = вміст producer_brain_prompt.md.
• Підвантажити БАЗУ ЗНАНЬ у system (з prompt caching — великі й статичні):
  bot_brain.md + newlook_method.md + hooks_bank.md.
• Обрати сильну модель (звірити id), max_tokens вище (контент довгий).
• Перевірка: бот вітається як Юстина, веде Фундамент, пише її тоном.
РЕЗУЛЬТАТ: жива Юстина в чаті, знає весь метод. Вже можна тестувати!
```

## ФАЗА 1 — ПАМ'ЯТЬ КЛІЄНТКИ (профіль) ⭐ фундамент якості
```
• Розширити "нотатки" у структурований ПРОФІЛЬ (JSON на user_id):
  ДНК / ЦА+карта сенсів / конкуренти / позиціонування / продукт /
  контент-план / цілі / стан / "що працює".
• Tools: save_profile_field, get_profile, update_profile.
• Профіль ІНЖЕКТИТЬСЯ в кожен запит (бот пам'ятає, не перепитує).
• Персистити ІСТОРІЮ на диск (не втрачати при рестарті).
РЕЗУЛЬТАТ: "ниточка" працює — бот пам'ятає клієнтку між сесіями.
```

## ФАЗА 2 — МЕНЮ Й НАВІГАЦІЯ
```
• Inline-клавіатура: головне меню (🌱💎✍️⚙️ + швидкі дії).
• /start = онбординг (вступ → "навіщо блог") → меню.
• Кнопки режимів заводять у потрібний потік.
• "👤 Мій кабінет" — показ збереженого профілю.
РЕЗУЛЬТАТ: зрозуміла навігація, не "порожній чат".
```

## ФАЗА 3 — ЗОБРАЖЕННЯ В КОНТЕКСТІ
```
• Інтегрувати vision в агент-цикл (зараз окремий handler, губить контекст).
• Сценарії скрінів: конкуренти (аналіз) / Insights (аналіз статистики) /
  переписка директу (ко-пілот).
РЕЗУЛЬТАТ: аналіз конкурентів, статистики, підказки в директі.
```

## ФАЗА 4 — ПРОАКТИВНІСТЬ (ядро задуму)
```
• Планувальник (JobQueue у python-telegram-bot або APScheduler).
• Щоранкові / тижневі повідомлення за профілем+контент-планом.
• 6 сценаріїв (ранок/вечір/тиждень/запуск/трекер/реактивація).
• Опція вкл/викл + час нагадувань у профілі.
РЕЗУЛЬТАТ: Юстина пише САМА — головна відмінність від AI-чату.
```

## ФАЗА 5 — ШЛІФУВАННЯ (паралельно й далі)
```
• AI-тон + голос клієнтки (кидає пости → бот вчить манеру).
• Генератори форматів (рілс/карусель/сторіс), репакер 1→10.
• Автоворонка-хелпери (ManyChat/SendPulse інструкції, лід-магніт).
• Тестування ВЖИВУ на реальних людях → правки в мозок.
• AI-суперсили з roadmap за пріоритетом.
```

## ПОРЯДОК І ЛОГІКА
```
Фаза 0 → одразу маємо що показати й тестувати.
Фаза 1 → якість (пам'ять) — без неї бот "тупий".
Фаза 2 → зручність (меню).
Фаза 3 → скріни (конкуренти/статистика).
Фаза 4 → проактивність (утримання).
Фаза 5 → нескінченне вдосконалення.
Деплой на кожній фазі: git push → сервер сам підтягує й перезапускає.
```

## ВІДКРИТІ ТЕХ-ПИТАННЯ (вирішити на початку коду)
```
• Актуальний id моделі (звірити; взяти сильну для якості контенту).
• Розмір бази знань у контексті vs RAG — для MVP, ймовірно, все в
  system з prompt caching (просто й дешево при кешуванні).
• Формат пам'яті: JSON-файли (просто) чи SQLite (надійніше). Для MVP — JSON.
• Один головний файл (bot.py) — прибрати дублювання bot.py/agent_bot.py.
```
